      <h1 class="banner-title">
        The Tech Blog
      </h1>
    </header>
    <main>


{{#if isEmpty}}
  <div class="card">
    <div class="card-title">
      <h2>No posts exist</h2>
    </div>
  </div>
{{/if}}
{{#each posts as |post|}}
  <div class="card">
    <div class="card-title">
      <h2>{{title}}</h2>
    </div>
    <div class="card-body">
      <p>{{text}}</p>
    </div>
  </div>
{{/each}}